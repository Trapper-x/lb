#ifndef IU_STRINGS_INCLUDE
#define IU_STRINGS_INCLUDE

#include <stdio.h>
#include <iostream>
#include <vector>
#include <algorithm>
#include <cstring>

char* stringCopy(const char*);
char* stringConcatinate(const char*, const char*);

#endif // IU_STRINGS_INCLUDE